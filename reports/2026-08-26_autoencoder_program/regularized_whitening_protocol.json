{
  "date": "2026-09-05",
  "status": "complete",
  "question": "Can an invertible ordered whitening readout remove nuisance covariance without amplifying the weak tail or damaging decode numerics, so that tokenwise SNR timing and loss allocation can later be reintroduced as controlled interventions?",
  "source": {
    "tokenizer": "v27-residual-e7p1-det-jitter05-slotbal2e3-n64d16-s2",
    "prior_normalization": "v27-residual-e7p1-det-jitter05-slotbal2e3-n64d16-s2-prior-s1",
    "physical_shape": [64, 16],
    "fit_split": "first 25000 training latents",
    "evaluation_split": "all 10000 disjoint test latents"
  },
  "candidate_readouts": {
    "factorized": "Rotate by the population sequence and channel eigenvectors, retain sequence-mode rank as the 64-token order, estimate all 1024 product-coordinate powers, and regularize their diagonal whitening gains.",
    "flattened": "Rotate by the complete 1024-dimensional population PCA basis, regularize its diagonal whitening gains, and pack consecutive eigenvalue ranks into 64 tokens of width 16."
  },
  "whitening": {
    "gain_formula": "g_j = 1/sqrt(max(power_j, max(power)/cap^2)), followed by one scalar normalization so mean transformed training power is one",
    "relative_gain_caps": [4, 8, 16, 32],
    "selected_cap_must_not_exceed": 16,
    "clean_magnitude_policy": "The stored prior coordinates are approximately unit scale. Decoder inversion restores the original physical latent gauge; no magnitude hierarchy is transmitted through the shared prior projections."
  },
  "diagnostics": [
    "complete held-out transformed covariance spectrum and effective rank",
    "held-out diagonal variance distribution and off-diagonal Frobenius fraction",
    "complete forward-gain distribution",
    "float32 and simulated float16-cache round-trip latent error",
    "decoded pixel error after simulated float16-cache round trip",
    "per-token pre-whitening power and all proposed schedule/loss profiles"
  ],
  "tempered_interventions": {
    "power_floor": "Use the same max(power)/cap^2 floor as the selected whitening cap before deriving token profiles, preventing a near-null terminal direction from dictating the schedule.",
    "beta_grid": [0.0, 0.125, 0.25, 0.5],
    "rational_odds": "a_i = normalized_floored_token_power_i^(beta/2)",
    "snr1_crossing": "1/(1+a_i)",
    "signal_metric_loss": "mean-one weights proportional to normalized_floored_token_power_i^beta",
    "flow_target_energy_loss": "mean-one weights proportional to 1 + normalized_floored_token_power_i^beta",
    "note": "These loss families are alternatives in the audit; no training combines both."
  },
  "health_gates": {
    "finite": "all transforms and metrics must be finite",
    "float32_relative_roundtrip_rms_max": 0.00001,
    "float16_relative_roundtrip_rms_max": 0.002,
    "decoded_float16_pixel_delta_rms_max": 0.002,
    "covariance": "selected transform must increase held-out covariance effective rank and reduce off-diagonal Frobenius fraction relative to the untransformed centered latent",
    "gain": "selected relative forward-gain range must be at most 16x",
    "schedule_family": "selected softened family must have at most 4x rational-odds range, at most 16x signal-metric loss range, and at most 3x flow-target-energy loss range"
  },
  "selection_rule": "Prefer the factorized sequence-ordered transform when it passes every health gate. Use flattened PCA only if factorized fails or leaves clearly pathological held-out covariance. Among healthy caps no larger than 16, prefer the smallest cap achieving at least 95% of the held-out effective-rank improvement attained by cap 16. Select the strongest beta that passes all schedule-family bounds; beta 0 remains the exact common-time/uniform-loss control. If both loss profiles pass, use the gentler flow-target-energy profile for the weighted training arm because the actual rectified-flow target is clean minus unit noise; retain signal-metric weights as a reported sensitivity diagnostic rather than an additional arm.",
  "training_authorization": "No prior training is launched until this audit is complete and its result freezes one transform, cap, beta, token crossings, and one loss-weight family. A pass authorizes the four common/ordered time by uniform/weighted loss arms on one exact transformed cache, retaining the unwhitened v27 common-time prior as the external baseline.",
  "selection_metrics": "Whitening diagnostics are safety and identifiability gates, not evidence of generative improvement. Any later model selection remains decoded FID/KID; reconstruction is only a health veto.",
  "gpu_protocol": "/workspace/GPU_QUEUEING.md; gpu-claim lifetime lock; supervisor-owned run",
  "execution": {
    "final_code_commit": "f02a1e0",
    "supervisor_job": "afig_regularized_whitening",
    "gpu": 1,
    "completed_utc": "2026-09-05T02:22:28Z",
    "focused_tests": "33 passed",
    "implementation_correction": "The first pass exposed TF32 and a 1.55e-5 float32 sequence-basis orthogonality defect against the frozen 1e-5 inverse gate. The final pass disabled TF32 and QR-repaired the numerical representation of the same ordered eigenvectors; no gate or scientific choice changed."
  },
  "result": {
    "readout": "factorized sequence/channel product basis",
    "relative_gain_cap": 16,
    "heldout_effective_rank": 794.98388671875,
    "baseline_effective_rank": 369.8818664550781,
    "heldout_off_diagonal_frobenius_fraction": 0.5843778848648071,
    "baseline_off_diagonal_frobenius_fraction": 0.9439206719398499,
    "float32_relative_roundtrip_rms": 7.094821367903688e-7,
    "float16_relative_roundtrip_rms": 0.00020633806480899688,
    "decoded_float16_pixel_delta_rms": 0.0012106811287174756,
    "selected_beta": 0.25,
    "snr1_crossing_range": [0.4332219152622271, 0.6045426889568087],
    "rational_odds_range": 2.0,
    "loss_family": "flow_target_energy",
    "loss_weight_range": 1.899016862917737,
    "verdict": "All frozen gates pass. Freeze the exact factorized cap-16 transform and beta-.25 schedule/flow-target weights; authorize the four-arm matched prior factorial."
  },
  "result_document": "regularized_whitening/results.md"
}
